{
  "name": "marketing-agents-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "docs:serve": "mkdocs serve",
    "docs:build": "mkdocs build",
    "docs:build:strict": "mkdocs build --strict"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11"
  }
}
